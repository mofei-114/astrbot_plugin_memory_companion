{
  "appearance": {
    "description": "拓展页外观",
    "hint": "控制记忆放映馆拓展页的配色主题。",
    "type": "object",
    "items": {
      "theme": {
        "description": "配色主题",
        "hint": "选择拓展页使用的四色主题。默认月白。",
        "type": "string",
        "options": [
          "黄白游",
          "天缥",
          "海天霞",
          "盈盈",
          "欧碧",
          "青冥",
          "紫蒲",
          "山岚",
          "窃蓝",
          "退红",
          "葱倩",
          "月白",
          "墨黪",
          "骨缥"
        ],
        "default": "月白"
      }
    }
  },
  "memory_capture": {
    "description": "记忆捕获",
    "hint": "控制插件是否记录用户消息、Bot 回复和轻量身份信息。",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用记忆捕获",
        "hint": "关闭后不会自动写入新记忆，但管理命令和手动写入仍可使用。",
        "type": "bool",
        "default": true
      },
      "capture_user_messages": {
        "description": "记录用户消息",
        "hint": "记录触发主链 LLM 的用户消息到时间线，供阶段性总结使用；默认不直接写入长期记忆。",
        "type": "bool",
        "default": true
      },
      "capture_bot_responses": {
        "description": "记录 Bot 回复",
        "hint": "记录 Bot 回复到时间线，供阶段性总结使用；默认不直接写入长期记忆。",
        "type": "bool",
        "default": true
      },
      "capture_min_chars": {
        "description": "最短记录字数",
        "hint": "短于该长度的普通文本不自动记录。命令、纯符号和空消息也会跳过。",
        "type": "int",
        "default": 2
      },
      "extract_stable_facts": {
        "description": "抽取稳定事实",
        "hint": "从明确表达中抽取偏好、称呼、生日、记住事项等稳定事实。不会把含糊猜测当事实。",
        "type": "bool",
        "default": true
      },
      "record_relationship_edges": {
        "description": "记录关系边",
        "hint": "记录群成员归属等结构化关系。身份冒领类关系声明不会自动沉淀为稳定记忆。",
        "type": "bool",
        "default": true
      }
    }
  },
  "portrait": {
    "description": "统一用户画像",
    "hint": "画像事实、证据、敏感等级和检索前隐私裁决由 Memory 维护；人物身份、关系和私聊权限仍由 Companion 维护。",
    "type": "object",
    "items": {
      "min_independent_evidence": {
        "description": "智能画像独立证据阈值",
        "type": "int",
        "default": 3,
        "hint": "同一人物、同一候选至少三条去重后的本人消息证据；不要求与 Bot 互动或跨会话。"
      },
      "daily_success_limit_per_person": {
        "description": "单用户每日成功更新上限",
        "type": "int",
        "default": 1,
        "hint": "智能画像每天最多成功更新一次。"
      },
      "daily_attempt_limit_per_person": {
        "description": "单用户每日尝试上限",
        "type": "int",
        "default": 2,
        "hint": "允许一次失败退避重试；达到上限后当天不再生成。"
      },
      "daily_dispatch_enabled": {
        "description": "启用每日画像批处理",
        "type": "bool",
        "default": true,
        "hint": "只在设定窗口扫描已有证据队列；不会在每条消息后调用画像模型。"
      },
      "update_window_start_hour": {
        "description": "画像更新窗口开始小时",
        "type": "int",
        "default": 3,
        "hint": "按 Asia/Shanghai 时区执行每日批处理。"
      },
      "update_window_end_hour": {
        "description": "画像更新窗口结束小时",
        "type": "int",
        "default": 5,
        "hint": "窗口外只收集哈希化证据，不整理智能画像。"
      },
      "token_budget_per_person_day": {
        "description": "单用户每日画像 Token 预算",
        "type": "int",
        "default": 4000,
        "hint": "预留给后续经批准的批量推断；当前确定性证据聚合不调用画像 LLM。"
      },
      "token_budget_global_day": {
        "description": "全局每日画像 Token 预算",
        "type": "int",
        "default": 40000,
        "hint": "预留给后续经批准的批量推断；预算不足时必须跳过而不是降级为逐消息调用。"
      },
      "usage_min_confidence": {
        "description": "智能画像使用置信度",
        "type": "float",
        "default": 0.75,
        "hint": "低于此值的智能推断不会进入表达适配。"
      },
      "inferred_freshness_days": {
        "description": "智能推断新鲜期",
        "type": "int",
        "default": 90,
        "hint": "超过期限的智能推断停止使用，等待新的独立证据。"
      },
      "context_message_limit": {
        "description": "画像上下文消息上限",
        "type": "int",
        "default": 8,
        "hint": "后续需要语义消歧时只允许使用同场景最小窗口；第三方消息不能作为事实证据。"
      },
      "context_char_limit": {
        "description": "画像上下文字符上限",
        "type": "int",
        "default": 6000,
        "hint": "上下文仅作当场解释，不写入画像表，也不跨场景拼接。"
      }
    }
  },
  "memory_summary": {
    "description": "长期记忆总结",
    "hint": "把时间线整理为可长期保存、可召回的阶段性记忆。模型、触发阈值和证据长度都在这里配置。",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用阶段性总结",
        "hint": "开启后，同一会话累计到阈值时会调用当前会话模型生成 conversation_summary 长期记忆。",
        "type": "bool",
        "default": true
      },
      "provider_id": {
        "description": "总结模型提供商",
        "_special": "select_provider",
        "hint": "选择用于阶段性记忆总结的 LLM 提供商。留空则使用当前会话正在使用的模型。",
        "type": "string",
        "default": ""
      },
      "fallback_provider_id": {
        "description": "备用总结模型提供商",
        "_special": "select_provider",
        "hint": "主总结模型不可用或总结失败时使用。留空则直接回退当前会话模型。",
        "type": "string",
        "default": ""
      },
      "min_events": {
        "description": "最少事件数",
        "hint": "未总结的时间线事件少于该数量时不会总结。",
        "type": "int",
        "default": 20
      },
      "trigger_event_count": {
        "description": "按条数触发",
        "hint": "未总结事件达到该数量时触发总结。建议 12-20。",
        "type": "int",
        "default": 20
      },
      "trigger_interval_minutes": {
        "description": "按时间触发",
        "hint": "最早未总结事件距今超过该分钟数且达到最少事件数时触发总结。0 表示只按条数触发。",
        "type": "int",
        "default": 60
      },
      "max_events_per_summary": {
        "description": "单次最多事件",
        "hint": "单次交给模型总结的时间线事件上限，避免输入过长。",
        "type": "int",
        "default": 40
      },
      "max_input_chars": {
        "description": "总结输入字数",
        "hint": "单次总结最多发送给模型的时间线消息字符数。",
        "type": "int",
        "default": 6000
      },
      "max_summary_chars": {
        "description": "总结记忆字数",
        "hint": "生成的长期记忆内容最大字符数。",
        "type": "int",
        "default": 1200
      },
      "provider_timeout_seconds": {
        "description": "总结模型超时秒数",
        "hint": "单次调用总结模型的最长等待时间。超时会尝试备用模型并保留未总结时间线。0 表示不限制。",
        "type": "int",
        "default": 60
      },
      "evidence_events": {
        "description": "证据事件条数",
        "hint": "长期总结记忆中最多保留多少条原始时间线事件作为 evidence，便于排查总结来源。",
        "type": "int",
        "default": 12
      },
      "max_retries": {
        "description": "失败重试次数",
        "hint": "同一会话阶段性总结连续失败达到该次数后，连接、超时和限流错误会进入冷却并自动恢复探测；内容或解析错误暂停自动重试。原始时间线始终保留，也可手动强制重试。",
        "type": "int",
        "default": 3
      },
      "retry_backoff_seconds": {
        "description": "自动重试退避秒数",
        "hint": "首次自动重试前至少等待的秒数；连续失败会逐次延长，避免短暂断连时立即耗尽全部次数。0 表示不等待。",
        "type": "int",
        "default": 60
      },
      "transient_retry_cooldown_minutes": {
        "description": "连接错误冷却分钟",
        "hint": "连接、超时、限流或 502-504 达到失败上限后等待多久再自动探测。期间不会重复刷警告，手动总结不受影响。0 表示下一次检查即可探测。",
        "type": "int",
        "default": 10
      }
    }
  },
  "historical_chat_import": {
    "description": "历史对话整理导入",
    "hint": "上传 UTF-8 对话文本，确认用户/Bot 身份后按时间和会话片段逐步整理；原始时间线默认永久保留，并支持暂停、恢复和整批回滚。",
    "type": "object",
    "items": {
      "max_retries": {
        "description": "片段失败重试次数",
        "hint": "单个历史会话片段连续失败达到该次数后暂停整个批次，保留已完成进度。",
        "type": "int",
        "default": 3
      },
      "qq_max_range_days": {
        "description": "QQ 单次读取天数",
        "hint": "通过 NapCat/OneBot 直接读取好友历史时允许选择的最长时间范围，硬上限为 31 天。",
        "type": "int",
        "default": 31
      },
      "qq_max_messages": {
        "description": "QQ 单次消息上限",
        "hint": "一个读取预览最多暂存的好友消息数量，硬上限为 5000；超出时会在预览中明确标记截断。",
        "type": "int",
        "default": 5000
      },
      "qq_page_size": {
        "description": "QQ 历史分页条数",
        "hint": "每次调用 get_friend_msg_history 请求的消息数量，最大为 100。",
        "type": "int",
        "default": 100
      },
      "qq_max_pages": {
        "description": "QQ 历史扫描页数",
        "hint": "从最新消息向前定位所选时段时最多扫描的页数，达到上限会停止并提示记录可能不完整。",
        "type": "int",
        "default": 100
      },
      "qq_request_timeout_seconds": {
        "description": "QQ 接口超时秒数",
        "hint": "单次 OneBot 能力检测或历史分页请求的等待时间。",
        "type": "int",
        "default": 15
      },
      "merge_seconds": {
        "description": "连续发言合并秒数",
        "hint": "同一说话人在该秒数内连续发送的消息会合并成一次逻辑发言；原始消息仍分别保存。",
        "type": "int",
        "default": 120
      },
      "hard_gap_minutes": {
        "description": "会话硬分段分钟",
        "hint": "相邻发言超过该间隔时一定开始新的会话片段。",
        "type": "int",
        "default": 120
      },
      "soft_gap_minutes": {
        "description": "会话软分段分钟",
        "hint": "超过该间隔且遇到跨日、晚安、离开或重新问候时开始新片段。",
        "type": "int",
        "default": 30
      },
      "max_turns": {
        "description": "单片段逻辑发言数",
        "hint": "限制一个会话片段最多包含多少次逻辑发言，避免提示过长。",
        "type": "int",
        "default": 30
      },
      "max_segment_chars": {
        "description": "单片段正文字数",
        "hint": "单个会话片段的正文上限；切分时尽量不拆开用户提问与 Bot 回答。",
        "type": "int",
        "default": 4200
      },
      "package_chars": {
        "description": "单次总结打包字数",
        "hint": "允许把多个较短片段打包到一次模型请求中，但每个片段仍独立返回摘要和事件。",
        "type": "int",
        "default": 4600
      },
      "detail_package_chars": {
        "description": "详细回忆补全打包字数",
        "hint": "旧批次升级为第三人称详细回忆时，单次发送给总结模型的片段与原文总量。数值越大调用更少，但更容易超时。",
        "type": "int",
        "default": 9000
      }
    }
  },
  "retrieval": {
    "description": "记忆召回模型",
    "hint": "控制长期记忆如何被找出来：嵌入模型负责语义候选，重排模型负责二阶段排序；权限和窗口隔离始终优先。",
    "type": "object",
    "items": {
      "embedding_enabled": {
        "description": "启用嵌入召回",
        "hint": "开启后使用 Embedding Provider 为长期记忆建立向量索引，并额外召回语义相近候选；失败时自动回退本地检索。",
        "type": "bool",
        "default": false
      },
      "embedding_provider_id": {
        "description": "嵌入模型 Provider",
        "hint": "选择 AstrBot 中 provider_type=embedding 的 Provider。留空时自动使用第一个可用嵌入模型。",
        "type": "string",
        "default": ""
      },
      "mode": {
        "description": "重排使用方式",
        "hint": "auto 推荐；basic 不调用重排；rerank 会强制尝试下方重排 Provider，失败时仍回退本地排序。",
        "type": "string",
        "options": [
          "auto",
          "basic",
          "rerank"
        ],
        "default": "auto"
      },
      "rerank_provider_id": {
        "description": "重排模型 Provider",
        "hint": "选择 AstrBot 中具备 rerank() 能力的 Provider。留空时 auto 自动扫描第一个可用重排模型。",
        "type": "string",
        "default": ""
      },
      "rerank_candidate_multiplier": {
        "description": "重排候选倍率",
        "hint": "实际注入 TopK 的多少倍进入二阶段重排。建议 3-5；过大会增加重排耗时。",
        "type": "int",
        "default": 5
      },
      "rerank_candidate_limit": {
        "description": "重排候选上限",
        "hint": "每轮最多送入重排模型的候选记忆数量。建议 24-40。",
        "type": "int",
        "default": 32
      }
    }
  },
  "retrieval_advanced": {
    "description": "检索高级参数",
    "hint": "向量扫描上限、相似度阈值、超时和补索引等高级调优参数。日常使用可保持默认。",
    "type": "object",
    "items": {
      "embedding_candidate_limit": {
        "description": "向量扫描上限",
        "hint": "每轮最多从本地向量索引中扫描多少条候选。记忆库很大时可适当降低；想提高旧记忆召回覆盖面可提高。",
        "type": "int",
        "default": 1200
      },
      "embedding_top_k": {
        "description": "向量合并数量",
        "hint": "向量相似度最高的多少条会并入本地候选池，再经过权限过滤、本地评分和可选重排。",
        "type": "int",
        "default": 32
      },
      "embedding_score_threshold": {
        "description": "向量相似度阈值",
        "hint": "低于该相似度的向量结果不会进入候选池。建议 0.30-0.42；过高会漏召回，过低会带来无关记忆。",
        "type": "float",
        "default": 0.34
      },
      "embedding_weight": {
        "description": "向量加权分",
        "hint": "向量相似度在最终本地评分中的软加分权重。建议 0.45-0.75；重排启用时不宜过高。",
        "type": "float",
        "default": 0.55
      },
      "embedding_timeout_ms": {
        "description": "嵌入超时毫秒",
        "hint": "生成查询向量或记忆向量超过该时间会回退/跳过。0 表示不限制；OpenAI 兼容公益站建议 4000-8000。",
        "type": "int",
        "default": 5000
      },
      "rerank_timeout_ms": {
        "description": "重排超时毫秒",
        "hint": "重排模型超过该时间会回退本地检索。0 表示不限制；测试期建议 800-1500。",
        "type": "int",
        "default": 1200
      },
      "embedding_max_text_chars": {
        "description": "嵌入文本上限",
        "hint": "送入 Embedding Provider 的单条记忆文本最大长度。会包含类型、范围、标签、内容、摘要字段和证据。",
        "type": "int",
        "default": 1200
      },
      "embedding_backfill_enabled": {
        "description": "补齐旧记忆向量",
        "hint": "开启嵌入召回后，每次检索会小批量补齐旧记忆的向量索引，不阻塞当前回复。",
        "type": "bool",
        "default": true
      },
      "embedding_backfill_batch_size": {
        "description": "补向量批量",
        "hint": "每次后台最多补齐多少条旧记忆向量。值越大，首次开启后建索引越快，但会增加 Embedding Provider 调用量。",
        "type": "int",
        "default": 50
      },
      "embedding_backfill_interval_seconds": {
        "description": "补向量最短间隔",
        "hint": "同一 Embedding Provider 两次后台缺失扫描之间的最短秒数，避免每轮检索都重复扫描。0 表示不节流。",
        "type": "int",
        "default": 300
      },
      "embedding_background_concurrency": {
        "description": "向量后台并发",
        "hint": "后台新增记忆和批量补索引共用的最大并发数，建议 1-3。",
        "type": "int",
        "default": 2
      },
      "current_window_candidate_limit": {
        "description": "当前窗口候选保底",
        "hint": "无论全局记忆数量多少，额外读取当前私聊或群聊窗口的候选上限，防止高重要度全局记录挤掉当前上下文。",
        "type": "int",
        "default": 600
      },
      "keyword_fallback_min_fts_candidates": {
        "description": "宽泛关键词回退阈值",
        "hint": "FTS 候选少于该数量时才执行多列 LIKE 回退；设为 0 可完全关闭宽泛 LIKE 回退。",
        "type": "int",
        "default": 80
      }
    }
  },
  "memory_injection": {
    "description": "记忆注入包",
    "hint": "控制每轮主链请求前注入多少记忆、最多多少字，以及是否记录注入日志。",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用记忆注入",
        "hint": "开启后会在主链 LLM 请求前注入 MemoryCompanion 记忆包；关闭后只保留记录、检索命令和工具读取。",
        "type": "bool",
        "default": true
      },
      "top_k": {
        "description": "注入条数",
        "hint": "每轮最多注入多少条记忆。建议 3-8。",
        "type": "int",
        "default": 6
      },
      "max_chars": {
        "description": "最大注入字数",
        "hint": "限制单轮注入长度，避免拖慢主链和污染上下文。",
        "type": "int",
        "default": 1800
      },
      "temporal_aggregate_max_chars": {
        "description": "时间聚合注入字数",
        "hint": "当用户问最近一周、最近几天、本周这类需要汇总多条记忆的问题时使用。默认更宽，且会用精简证据格式注入。",
        "type": "int",
        "default": 3600
      },
      "include_raw_events": {
        "description": "允许注入原始事件",
        "hint": "默认关闭。关闭后只注入总结、稳定事实和外部稳定记录，避免流水账污染回复。",
        "type": "bool",
        "default": false
      },
      "enable_injection_logs": {
        "description": "记录注入日志",
        "hint": "记录每轮选中了哪些记忆、过滤了哪些候选和原因，便于排障。",
        "type": "bool",
        "default": true
      },
      "debug_log_injection_enabled": {
        "description": "日志打印注入内容",
        "hint": "默认关闭。仅在排障时临时开启；开启后会把检索 query、命中记忆、过滤原因、连续对话记忆状态和实际注入包打印到 AstrBot 日志。",
        "type": "bool",
        "default": false
      },
      "debug_log_max_chars": {
        "description": "注入调试日志字数",
        "hint": "单轮注入调试日志最多打印多少字符。调大可查看完整注入块，调小可避免日志过长。",
        "type": "int",
        "default": 12000
      }
    }
  },
  "conversation_memory": {
    "description": "连续对话记忆",
    "hint": "记录群聊中连续发生的可见对话，阶段性整理为 conversation_summary，用于之后衔接话题；不会把原始消息窗口直接注入给模型。",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用连续对话记忆",
        "hint": "开启后会记录群聊连续发言，并通过阶段性总结沉淀为可召回的连续对话记忆。",
        "type": "bool",
        "default": true
      },
      "capture_group_messages": {
        "description": "记录群聊普通发言",
        "hint": "开启后，即使 Bot 没被唤醒，也会记录群聊中的普通文本发言，用于形成连续对话片段。",
        "type": "bool",
        "default": true
      },
      "idle_gap_minutes": {
        "description": "连续片段间隔",
        "hint": "群聊相邻消息超过该分钟数时，视为新的连续对话片段。当前主要用于标记和后续展示。",
        "type": "int",
        "default": 20
      }
    }
  },
  "conversation_memory_advanced": {
    "description": "连续对话高级参数",
    "hint": "低信息保护、新话题隔离、追问补全和时间窗口等高级参数。日常使用可保持默认。",
    "type": "object",
    "items": {
      "recent_events_for_followup": {
        "description": "追问补全事件数",
        "hint": "处理\u201c还有的吧\u201d\u201c刚才那个\u201d等连续追问时，最多读取多少条最近群聊事件来补全检索词。",
        "type": "int",
        "default": 12
      },
      "time_window_timeline_limit": {
        "description": "时间窗口时间线条数",
        "hint": "当用户问今天、昨天、最近一周、上个月等时间问题时，最多补充多少条该窗口内的原始时间线事件。",
        "type": "int",
        "default": 12
      },
      "recent_fact_guard_enabled": {
        "description": "近期事实防遗忘",
        "hint": "开启后，从同一私聊近期时间线中提取少量吃饭、洗澡、上下班、位置和休息状态，轻量提醒模型避免重复询问；不会回灌整段聊天。",
        "type": "bool",
        "default": true
      },
      "recent_fact_guard_hours": {
        "description": "近期事实有效小时",
        "hint": "只参考该小时数内的私聊即时状态；更近的明确更新会覆盖较早状态。",
        "type": "int",
        "default": 3
      },
      "recent_fact_guard_event_limit": {
        "description": "近期事实扫描事件数",
        "hint": "每轮最多检查多少条同一私聊时间线事件，用于寻找即时状态和 Bot 已回应证据。",
        "type": "int",
        "default": 24
      },
      "recent_fact_guard_max_items": {
        "description": "近期事实最多条数",
        "hint": "每轮最多给模型多少条近期状态锚点；默认 4 条以控制提示长度和干扰。",
        "type": "int",
        "default": 4
      },
      "low_information_guard_enabled": {
        "description": "低信息输入保护",
        "hint": "开启后，摸摸、贴贴、嗯、哈哈等弱语义输入不会轻易继承过期话题。",
        "type": "bool",
        "default": true
      },
      "low_information_gap_minutes": {
        "description": "低信息过期分钟",
        "hint": "弱语义输入距离上一条同会话事件超过该分钟数时，会隔离旧话题。摸摸/贴贴类亲昵输入始终隔离旧话题。",
        "type": "int",
        "default": 20
      },
      "suppress_memory_on_low_information": {
        "description": "弱语义禁用长期召回",
        "hint": "开启后，弱语义输入不使用长期记忆和陪伴插件检索线索，只根据当前消息与必要的近期原始上下文回复。",
        "type": "bool",
        "default": true
      },
      "topic_shift_guard_enabled": {
        "description": "新话题隔离保护",
        "hint": "开启后，发自拍、上传图、生成图、查询等明确新请求若与最近历史无主题重叠，会隔离旧话题，避免旧话题抢答。",
        "type": "bool",
        "default": true
      },
      "suppress_memory_on_topic_shift": {
        "description": "新话题禁用长期召回",
        "hint": "开启后，新话题隔离触发时也会跳过长期记忆召回；用户明确说记得、之前、上次、按我喜欢的等需要记忆时不会跳过。",
        "type": "bool",
        "default": true
      },
      "topic_shift_guard_recent_events": {
        "description": "新话题比较事件数",
        "hint": "判断新请求是否与最近历史重叠时，最多比较多少条同会话时间线事件。",
        "type": "int",
        "default": 6
      },
      "group_actor_relevance_guard_enabled": {
        "description": "群聊发言者相关性保护",
        "hint": "开启后，群聊不会因普通请求带出其他成员画像；获准共享的私聊记忆只在明确回忆或个性化请求中，且当前发言者或点名对象匹配时注入。",
        "type": "bool",
        "default": true
      },
      "cross_window_recent_continuity_enabled": {
        "description": "跨窗口近期衔接",
        "hint": "开启后，为同一平台、同一 Bot 和同一用户生成少量短时上下文片段，使群聊与私聊切换后可以自然续接；不会回灌完整聊天记录。",
        "type": "bool",
        "default": true
      },
      "cross_window_recent_minutes": {
        "description": "跨窗口衔接有效分钟",
        "hint": "只读取该分钟数内相反会话类型的近期消息，过期内容不会进入跨窗口上下文。",
        "type": "int",
        "default": 30
      },
      "cross_window_recent_event_limit": {
        "description": "跨窗口衔接片段数",
        "hint": "每轮最多提供多少条当前用户与同一 Bot 的近期往返片段。默认 6 条，避免把原始历史整段塞给模型。",
        "type": "int",
        "default": 6
      },
      "cross_window_group_to_private_enabled": {
        "description": "群聊衔接到私聊",
        "hint": "开启后，当前用户参与的近期群聊内容可在其私聊中作为语义续接参考。",
        "type": "bool",
        "default": true
      },
      "cross_window_private_to_group_enabled": {
        "description": "允许私聊授权后衔接群聊",
        "hint": "开启后，当前用户可在群聊本轮明确授权，把自己的近期私聊话题带到群里；明确授权始终是必要条件。关闭后完全禁用该方向。",
        "type": "bool",
        "default": true
      }
    }
  },
  "context_orchestration": {
    "description": "注入编排",
    "hint": "把自我时间线、用户画像、当前窗口、阶段总结和稳定记忆分槽编排到最终注入包。",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用分槽调度",
        "hint": "开启后按分槽检索与注入；关闭时退回普通稳定记忆检索。",
        "type": "bool",
        "default": true
      },
      "query_mode": {
        "description": "检索架构模式",
        "hint": "current_message 为默认安全模式，只用当前用户消息检索；guarded_companion 仅在陪伴线索与当前消息有主题重叠时扩展检索；companion_augmented 为旧式增强检索，会直接拼接陪伴线索。",
        "type": "string",
        "options": [
          "current_message",
          "guarded_companion",
          "companion_augmented"
        ],
        "default": "current_message"
      },
      "include_intent_context": {
        "description": "注入检索意图",
        "hint": "在上下文包中加入本轮检索意图摘要，帮助模型理解这些记忆为什么被取出。",
        "type": "bool",
        "default": true
      },
      "current_state_relevance_guard_enabled": {
        "description": "当前状态问题相关性保护",
        "hint": "开启后，今天穿什么、现在在干嘛、吃什么、心情怎样这类当前状态问题，只允许直接命中对应锚点的长期记忆进入注入包，避免旧阶段总结抢答。",
        "type": "bool",
        "default": true
      }
    }
  },
  "context_orchestration_advanced": {
    "description": "编排高级参数",
    "hint": "各分槽条数上限和意图摘要字数等高级调优参数。日常使用可保持默认。",
    "type": "object",
    "items": {
      "intent_max_chars": {
        "description": "意图摘要字数",
        "hint": "限制检索意图摘要长度，避免陪伴插件线索过长。",
        "type": "int",
        "default": 520
      },
      "self_timeline_limit": {
        "description": "Bot 自我时间线槽",
        "hint": "每轮最多放入多少条 Bot 自己的日程、主动行为和陪伴笔记。",
        "type": "int",
        "default": 2
      },
      "user_profile_limit": {
        "description": "用户画像槽",
        "hint": "每轮最多放入多少条当前用户偏好、身份、关系和明确记住事项。",
        "type": "int",
        "default": 2
      },
      "current_window_limit": {
        "description": "当前窗口槽",
        "hint": "每轮最多放入多少条当前私聊或群聊窗口相关记忆。",
        "type": "int",
        "default": 3
      },
      "conversation_summary_limit": {
        "description": "阶段总结槽",
        "hint": "每轮最多放入多少条阶段性对话总结，用于保持连续性。",
        "type": "int",
        "default": 2
      },
      "stable_memory_limit": {
        "description": "稳定记忆槽",
        "hint": "每轮最多放入多少条其他可见稳定记忆；总数仍受记忆注入 top_k 限制。",
        "type": "int",
        "default": 3
      }
    }
  },
  "private_companion_bridge": {
    "description": "记忆插件协同",
    "hint": "与主动陪伴插件协调记忆、连续对话和自我时间线，减少重复注入和提示词污染。",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用桥接 API",
        "hint": "开启后插件实例会暴露 memory_companion 桥接对象，供其他插件记录主动消息、日程、创作、搜索等事件。",
        "type": "bool",
        "default": true
      },
      "schedule_fast_context_enabled": {
        "description": "启用日程快速上下文",
        "hint": "让陪伴插件读取日程连续性时使用本地只读候选，不调用 Embedding 或 Rerank；关闭后回退完整检索。",
        "type": "bool",
        "default": true
      },
      "outfit_fast_context_enabled": {
        "description": "启用穿搭快速上下文",
        "hint": "让每日穿搭仅快速读取相关日程、历史穿搭、服装偏好和自拍记录；关闭后回退完整检索。",
        "type": "bool",
        "default": true
      },
      "accept_external_records": {
        "description": "接受外部写入",
        "hint": "关闭后只保留本插件自动捕获和管理命令写入。",
        "type": "bool",
        "default": true
      },
      "enable_p5_b1_recall_gate": {
        "description": "启用 P5-B1 召回闸门",
        "hint": "默认关闭。开启后普通记忆注入和主动 recall 需要陪伴侧当前请求的一次性 attestation；无效时返回明确 degraded。",
        "type": "bool",
        "default": false
      },
      "enable_p5_b1_bridge_gate": {
        "description": "启用 P5-B1 Bridge 闸门",
        "hint": "默认关闭。开启后 Bridge 搜索、上下文和序列化入口需要一次性 attestation；不会删除原版娱乐写入。",
        "type": "bool",
        "default": false
      },
      "dedupe_prompt_context": {
        "description": "协同去重陪伴注入",
        "hint": "开启后会和陪伴插件协调，让重复的自我时间线、私聊上下文等段落只由一侧注入。",
        "type": "bool",
        "default": true
      },
      "prefer_memory_companion_memory": {
        "description": "MemoryCompanion 接管记忆上下文",
        "hint": "开启后陪伴插件会尽量让出自我时间线、私聊上下文和旧 LivingMemory 指引，由 MemoryCompanion 统一注入。",
        "type": "bool",
        "default": true
      },
      "preserve_external_prompt_context": {
        "description": "保留外部临时片段",
        "hint": "整理 AstrBot 原始历史时只保留真正带临时/不保存标记的外部片段；历史里遗留的 private_companion HTML 动态块会被清理，避免状态、引用链或工具提示写入会话历史后反复污染主链。",
        "type": "bool",
        "default": true
      },
      "cross_window_emotional_continuity_enabled": {
        "description": "跨窗口情绪连续性",
        "hint": "默认关闭。开启后其他会话中触发的情绪余波可影响当前窗口，但不会注入原始记忆内容。",
        "type": "bool",
        "default": false
      },
      "legacy_emotion_compatibility_enabled": {
        "description": "兼容旧版同窗口情绪读取",
        "hint": "过渡期开启。仅允许旧版陪伴插件读取并消费当前精确私聊窗口的脱敏情绪事件；跨窗口读取仍需新版能力上下文。升级陪伴插件后可关闭。",
        "type": "bool",
        "default": true
      },
      "clean_proactive_history": {
        "description": "清理主动消息历史提示",
        "hint": "注入前清理 AstrBot 原始上下文中残留的主动消息生成提示、send_message_to_user 工具说明、主动承接占位和 TTS/定时控制标签，只保留用户可见的干净聊天内容。",
        "type": "bool",
        "default": true
      },
      "suppress_self_timeline_when_companion_seen": {
        "description": "陪伴已注入时隐藏自我时间线槽",
        "hint": "检测到陪伴插件已提供状态或自我时间线时，隐藏重复的 Bot 自我时间线槽。",
        "type": "bool",
        "default": true
      },
      "suppress_user_context_when_companion_seen": {
        "description": "陪伴已注入时隐藏用户上下文槽",
        "hint": "检测到陪伴插件已提供私聊状态时，隐藏重复的用户画像和阶段总结槽。",
        "type": "bool",
        "default": true
      }
    }
  },
  "visibility": {
    "description": "隔离与可见性",
    "hint": "控制私聊、群聊、自我时间线之间的默认流动边界。",
    "type": "object",
    "items": {
      "allow_self_timeline_everywhere": {
        "description": "共享 Bot 自我时间线",
        "hint": "Bot 自己做过的动作可作为所有窗口的自我背景，但仍会带来源标签。",
        "type": "bool",
        "default": true
      },
      "allow_group_public_in_private": {
        "description": "私聊可见群公共记忆",
        "hint": "开启后私聊中可看到群公共记忆，便于跨窗口回话。如需严格隔离私聊与群聊可关闭。",
        "type": "bool",
        "default": false
      },
      "enable_acl_rules": {
        "description": "启用卡片权限",
        "hint": "开启后，可在私聊/群聊卡片中配置跨窗口读取权限；默认隔离仍然生效，只有显式授权的窗口会被额外放行。",
        "type": "bool",
        "default": true
      }
    }
  },
  "memory_tools": {
    "description": "主动记忆工具",
    "hint": "允许模型在需要时主动回忆、记忆和整理 Bot 自己的陪伴笔记。",
    "type": "object",
    "items": {
      "enable_recall_tool": {
        "description": "启用主动回忆工具",
        "hint": "开启后模型可调用 memory_companion_recall，按当前会话可见性主动检索记忆。",
        "type": "bool",
        "default": true
      },
      "enable_remember_tool": {
        "description": "启用主动记忆工具",
        "hint": "开启后模型可调用 memory_companion_remember 写入长期记忆；请配合总结防注入和召回日志观察效果。",
        "type": "bool",
        "default": true
      },
      "enable_note_tools": {
        "description": "启用陪伴笔记工具",
        "hint": "开启后模型可创建、读取和删除当前 Bot 自己可见的 companion_note，用于日程、状态、创作草稿和陪伴线索整理。",
        "type": "bool",
        "default": true
      },
      "enable_reconstruction_tool": {
        "description": "启用记忆导航工具",
        "hint": "开启后，模型可在普通召回证据不足时调用 memory_companion_navigate，按当前证据逐步补查；每一步仍会重新检查窗口、平台、Bot、权限和群发言者边界。",
        "type": "bool",
        "default": true
      }
    }
  },
  "memory_reconstruction": {
    "description": "主动记忆重建",
    "hint": "为明确回忆、时间、个性化和多跳问题提供有界的逐步证据导航。普通聊天仍使用原有快速检索。",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用主动重建",
        "hint": "开启后，复杂记忆问题可在首轮证据不足时继续导航；是否继续和何时停止由提示词约束。",
        "type": "bool",
        "default": true
      },
      "max_steps": {
        "description": "单轮最多导航步数",
        "hint": "同一条消息最多允许多少次不同的导航调用。默认 3；这是资源上限，不要求模型用满。",
        "type": "int",
        "default": 3
      },
      "per_step_limit": {
        "description": "每步证据条数",
        "hint": "每次导航最多返回多少条当前可见候选。默认 6，较小值可减少提示词体量。",
        "type": "int",
        "default": 6
      },
      "candidate_scan_limit": {
        "description": "每步图候选扫描数",
        "hint": "每步最多扫描多少条关联路径，再按当前权限过滤并截取可见证据。默认 96。",
        "type": "int",
        "default": 96
      }
    }
  },
  "knowledge_graph": {
    "description": "图谱关联",
    "hint": "从阶段性记忆抽取人物、话题和事实关联，用于辅助检索和拓展页排障。",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用图谱建索引",
        "hint": "开启后，阶段性总结成功时会自动写入知识图谱节点和边。",
        "type": "bool",
        "default": true
      },
      "retrieval_expansion_enabled": {
        "description": "检索使用图谱一跳扩展",
        "hint": "开启后，搜索某个人或话题时会从知识图谱取少量相关节点作为检索扩展词；最终结果仍受可见性和权限过滤。",
        "type": "bool",
        "default": true
      },
      "expansion_limit": {
        "description": "扩展词上限",
        "hint": "每次检索最多从图谱取多少个相关词。建议 6-12，过高会扩大召回范围。",
        "type": "int",
        "default": 12
      },
      "backfill_limit": {
        "description": "维护补建条数",
        "hint": "运行维护时，最多从多少条已有阶段性总结补建知识图谱。补建是幂等的，重复运行不会重复建边。",
        "type": "int",
        "default": 300
      }
    }
  },
  "livingmemory_migration": {
    "description": "LivingMemory 导入",
    "hint": "从旧 LivingMemory 数据库导入 documents 摘要。导入前建议预览并备份。",
    "type": "object",
    "items": {
      "import_limit": {
        "description": "单次导入上限",
        "hint": "单次最多导入多少条 LivingMemory documents 完整记忆。0 表示不限制。",
        "type": "int",
        "default": 5000
      },
      "livingmemory_db_path": {
        "description": "LivingMemory 数据库路径",
        "hint": "留空则自动扫描常见位置。建议先执行 /mcomp import_livingmemory preview。",
        "type": "string",
        "default": ""
      }
    }
  },
  "maintenance": {
    "description": "维护",
    "hint": "控制数据保留和清理策略。",
    "type": "object",
    "items": {
      "backup_before_import": {
        "description": "导入前备份",
        "hint": "执行旧记忆导入前先备份当前 SQLite 数据库。",
        "type": "bool",
        "default": true
      },
      "sleep_backup_enabled": {
        "description": "睡眠维护前备份",
        "hint": "开启后每次 /mcomp sleep run 或页面维护都会先备份数据库。默认关闭，避免频繁备份占用空间。",
        "type": "bool",
        "default": false
      },
      "retention_raw_event_days": {
        "description": "原始事件保留天数",
        "hint": "原始流水事件超过该天数后可进入清理。0 表示暂不自动清理。",
        "type": "int",
        "default": 7
      },
      "retention_raw_event_limit": {
        "description": "原始事件单次归档上限",
        "hint": "每次睡眠维护最多归档多少条过期 raw_event 长期记录，避免一次维护写入过多。",
        "type": "int",
        "default": 1000
      },
      "retention_summarized_timeline_days": {
        "description": "已总结时间线保留天数",
        "hint": "只清理已经成功写入长期总结的时间线；未总结和总结失败的原始消息不会被删除。0 表示不自动清理。",
        "type": "int",
        "default": 30
      },
      "retention_injection_log_days": {
        "description": "注入日志保留天数",
        "hint": "超过该天数的注入诊断日志会在睡眠维护时删除。0 表示不自动清理。",
        "type": "int",
        "default": 14
      },
      "retention_cleanup_limit": {
        "description": "保留清理单次上限",
        "hint": "每次维护分别最多删除多少条已总结时间线和注入日志，避免长事务。",
        "type": "int",
        "default": 2000
      },
      "memory_decay_enabled": {
        "description": "启用自然衰减",
        "hint": "开启后，睡眠维护会筛选长期未访问、重要度较低的稳定记忆，先压缩成高层摘要，再归档原碎片。",
        "type": "bool",
        "default": true
      }
    }
  },
  "maintenance_audit": {
    "description": "可回滚记忆审计",
    "hint": "根据摘要保存的原始事件引用生成修正建议。只生成预览，不会随睡眠维护自动执行；应用和回滚都需要明确确认。",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用记忆审计",
        "hint": "默认关闭。开启后管理员可手动生成审计预览，模型仍不能直接修改数据库。",
        "type": "bool",
        "default": false
      },
      "max_candidates": {
        "description": "单次候选上限",
        "hint": "每次预览最多检查多少条带原始事件引用的会话摘要。",
        "type": "int",
        "default": 20
      },
      "max_items_per_preview": {
        "description": "单次建议上限",
        "hint": "一次预览最多保留多少条通过本地证据校验的修改建议。",
        "type": "int",
        "default": 8
      },
      "provider_timeout_seconds": {
        "description": "审计模型超时秒数",
        "hint": "审计复用记忆总结模型，并按该时间限制单次请求。",
        "type": "int",
        "default": 60
      },
      "preview_expire_hours": {
        "description": "预览有效小时数",
        "hint": "超过有效期必须重新生成，避免应用已经过时的建议。",
        "type": "int",
        "default": 24
      }
    }
  },
  "maintenance_decay": {
    "description": "自然衰减高级参数",
    "hint": "衰减年龄、未访问天数、重要度阈值、候选上限和摘要字数等高级调优参数。日常使用可保持默认。",
    "type": "object",
    "items": {
      "memory_decay_after_days": {
        "description": "衰减年龄天数",
        "hint": "稳定记忆存在超过该天数后才可能进入衰减候选。明确记住、画像、偏好、关系、手动记忆会被保护。",
        "type": "int",
        "default": 180
      },
      "memory_decay_idle_days": {
        "description": "未访问天数",
        "hint": "记忆距离上次被召回超过该天数后才可能衰减；被频繁召回的记忆会自然加固。",
        "type": "int",
        "default": 90
      },
      "memory_decay_max_importance_percent": {
        "description": "最大衰减重要度",
        "hint": "重要度高于该百分比的记忆不会自动衰减。建议 65-80。",
        "type": "int",
        "default": 74
      },
      "memory_decay_max_access_count": {
        "description": "最大召回次数",
        "hint": "被召回次数超过该值的记忆不会自动衰减，表示它仍然有用。",
        "type": "int",
        "default": 2
      },
      "memory_decay_score_threshold_percent": {
        "description": "衰减分阈值",
        "hint": "年龄、未访问时间、重要度和置信度共同计算的衰减分达到该百分比后才进入压缩。",
        "type": "int",
        "default": 75
      },
      "memory_decay_max_candidates": {
        "description": "单次候选上限",
        "hint": "每次睡眠维护最多处理多少条衰减候选。",
        "type": "int",
        "default": 120
      },
      "memory_decay_max_groups": {
        "description": "单次窗口组上限",
        "hint": "每次睡眠维护最多压缩多少个私聊/群聊窗口，确保不同窗口的隐私边界不混合。",
        "type": "int",
        "default": 8
      },
      "memory_decay_min_items_per_summary": {
        "description": "摘要最少碎片",
        "hint": "同一窗口至少累积多少条衰减候选才生成压缩摘要并归档原碎片。",
        "type": "int",
        "default": 4
      },
      "memory_decay_max_items_per_summary": {
        "description": "单摘要碎片上限",
        "hint": "单条衰减摘要最多合并多少条旧记忆碎片。",
        "type": "int",
        "default": 24
      },
      "memory_decay_summary_input_chars": {
        "description": "衰减摘要输入字数",
        "hint": "单次衰减压缩最多发送给模型的旧记忆字符数。",
        "type": "int",
        "default": 6000
      },
      "memory_decay_summary_chars": {
        "description": "衰减摘要字数",
        "hint": "衰减压缩生成的新长期摘要最大字数。",
        "type": "int",
        "default": 900
      }
    }
  }
}
